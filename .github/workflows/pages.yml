# Optional: restore as .github/workflows/pages.yml after
# `gh auth refresh -s workflow` (OAuth currently lacks workflow scope).
# Until then Pages uses classic branch deploy from main /.

name: Deploy Squadrone su GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Inject config PAT
        env:
          SQUADRONE_PAT: ${{ secrets.SQUADRONE_PAT }}
        run: |
          if [ -n "$SQUADRONE_PAT" ]; then
            python3 - <<'PY'
          import json, os, pathlib
          pat = os.environ.get("SQUADRONE_PAT", "")
          cfg = {
            "owner": "rdagmr98",
            "repo": "squadrone-data",
            "branch": "main",
            "pat": pat,
            "adminPin": "1234",
          }
          pathlib.Path("js/config.js").write_text(
              "window.SQUADRONE_CONFIG = " + json.dumps(cfg, indent=2) + ";\n",
              encoding="utf-8",
          )
          print("config.js written with injected PAT")
          PY
          else
            echo "No SQUADRONE_PAT secret — users will set PAT from Impostazioni"
          fi

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - id: deployment
        uses: actions/deploy-pages@v4
